"""
Web content extraction tool.

Extracts readable content from web pages:
- HTML parsing via BeautifulSoup
- Main content extraction (strips nav, ads, etc.)
- Conversion to plain text or markdown
- Size limits to prevent context overflow
"""

